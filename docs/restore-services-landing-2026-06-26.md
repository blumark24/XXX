# Restore services landing

This commit intentionally triggers a fresh Vercel production deployment after restoring `main` to the services landing state before the footer upgrade.

No visible website UI changes are introduced by this file.
